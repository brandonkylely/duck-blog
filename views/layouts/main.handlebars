
{{! A boilerplate main layout is completed for you, feel free to modify and style to your liking }}

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>duck blog</title>
    {{!-- <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous"> --}}
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v6.3.0/css/all.css">
    <link rel="stylesheet" href="/css/style.css" />

  </head>

  <body class="text-gray-700 bg-white-yellow border-orange-sorbet">
        <header class="absolute">
          <h1 class="border-solid border-2 rounded-md border-orange-sorbet hover:border-t-4 hover:font-bold font-semibold text-4xl">duck blog</h1>
      </header>
    <nav class="flex flex-row justify-end">
        <a href="/" class="border-solid border-2 rounded-md border-slate-200 hover:border-t-4 hover:font-bold my-3 text-2xl p-1 mx-4">home</a>
        <a href="/dashboard" class="border-solid border-2 rounded-md border-slate-200 hover:border-t-4 hover:font-bold my-3 text-2xl p-1 mx-4"><i class="fa-solid fa-magnifying-glass"></i>dashboard</a>
        {{#unless loggedIn}}
        <a href="/login" class="border-solid border-2 rounded-md border-slate-200 hover:border-t-4 hover:font-bold my-3 text-2xl p-1 mx-4"><i class="fas fa-save"></i>login</a>
        {{/unless}}
        {{#if loggedIn}}
        <a href="#" id="logout-link" class="border-solid border-2 rounded-md border-slate-200 hover:border-t-4 hover:font-bold my-3 text-2xl p-1 mx-4">logout</a>
        {{/if}}
      </ul>
    </nav>
    <header>

    </header>

    <main>
      {{{body}}}
    </main>
  </body>
</html>
{{! inline script to handle click of logout button }}
<script>
  const logout = async function() { const response = await
  fetch('/api/user/logout', { method: 'POST', headers: { 'Content-Type':
  'application/json' }, }); if (response.ok) { document.location.replace('/'); }
  else { alert('Failed to log out'); } };
  document.querySelector('#logout-link').addEventListener('click', logout);
</script>
